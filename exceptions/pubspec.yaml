name: exceptions
description: The exceptions thrown during normal circumstances in the app.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  consts:
    path: ../consts

dev_dependencies:
  flutter_test:
    sdk: flutter
