name: padding
description: Padding definitions.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  distance:
    path: ../distance

dev_dependencies:
  flutter_test:
    sdk: flutter
