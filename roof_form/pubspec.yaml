name: roof_form
description: Roof's form tools.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  spec:
    path: ./../spec
  form:
    path: ./../form

dev_dependencies:
  flutter_test:
    sdk: flutter
