name: public_activity_artboard
description: Allows the user to see a public activity feed.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  vertical_full_screen_artboard_templates:
    path: ../../artboard_templates/vertical_full_screen
  sign_up_artboard:
    path: ../sign_up
  app_data:
    path: ../../app_data
  nav_button_builder:
    path: ../../builders/nav_button
  icon_library:
    path: ../../icon_library
  model:
    path: ../../model
  navigation:
    path: ../../navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
