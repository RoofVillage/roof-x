name: public_activity_artboards
description: The artboards that show public activity.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  full_screen_artboard_templates:
    path: ../../artboard_templates/full_screen
  auth_artboards:
    path: ../auth
  app_data:
    path: ../../app_data
  nav_button_builder:
    path: ../../builders/nav_button
  icon_library:
    path: ../../icon_library
  model:
    path: ../../model
  navigation:
    path: ../../navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
