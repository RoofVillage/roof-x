name: sign_up_artboard
description: Allows the user to create an account.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  vertical_floating_artboard_templates:
    path: ../../artboard_templates/vertical_floating
  form_builder:
    path: ../../builders/form
  app_data:
    path: ../../app_data
  challenge_artboard:
    path: ../challenge
  phone_number_sign_up_artboard:
    path: ../phone_number_sign_up
  navigation:
    path: ../../navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
