import 'package:flutter/material.dart';

class RoofCornerRadius {
  static const Radius small = Radius.circular(5);
  static const Radius regular = Radius.circular(10);
  static const Radius large = Radius.circular(15);
}
