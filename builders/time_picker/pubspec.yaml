name: time_picker_builder
description: Component for selecting a time of day

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  theme:
    path: ../../theme
  duration:
    path: ../../spec/duration
  curve:
    path: ../../spec/curve
  haptics:
    path: ../../lib/haptics
  typography:
    path: ../../spec/typography

dev_dependencies:
  flutter_test:
    sdk: flutter
