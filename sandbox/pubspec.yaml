name: Sandbox 
description: Testing.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  thread_components:
    path: ../thread_components

dev_dependencies:
  flutter_test:
    sdk: flutter
