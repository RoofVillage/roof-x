name: data_chunk

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mappable:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/mixins/mappable
  disk:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: lib/disk

dependency_overrides:
  disk:
    path: ../../lib/disk

dev_dependencies:
  flutter_test:
    sdk: flutter
