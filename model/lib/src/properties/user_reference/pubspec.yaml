name: user_reference

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  actor_reference:
    path: ../actor_reference

dev_dependencies:
  flutter_test:
    sdk: flutter
