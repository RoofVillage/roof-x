name: schedule

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  frequency_type:
    path: ../../../../../types/frequency
  mappable:
    path: ../../mixins/mappable
  key:
    path: ../../key

dev_dependencies:
  flutter_test:
    sdk: flutter
