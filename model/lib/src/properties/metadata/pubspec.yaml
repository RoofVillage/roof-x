name: metadata

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  event_meta_value_type:
    path: ../../../../../types/event_meta_value
  mappable:
    path: ../../mixins/mappable
  key:
    path: ../../key

dev_dependencies:
  flutter_test:
    sdk: flutter
