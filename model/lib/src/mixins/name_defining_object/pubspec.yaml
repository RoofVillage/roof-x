name: name_defining_object

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  strings: ^0.1.0
  name_user:
    path: ../../properties/name_user
  name_context:
    path: ../../properties/name_context
  user_reference:
    path: ../../properties/user_reference

dev_dependencies:
  flutter_test:
    sdk: flutter
