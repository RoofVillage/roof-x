name: model_object_cache

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  model_object:
    path: ../abstract/model_object
  disk:
    path: ../../lib/disk

dev_dependencies:
  flutter_test:
    sdk: flutter
