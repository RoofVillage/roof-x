name: model
description: Tools to reference and persist model objects.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  strings: ^0.1.0
  disk:
    path: ../lib/disk
  date:
    path: ../lib/date
  types:
    path: ../types

dev_dependencies:
  flutter_test:
    sdk: flutter
