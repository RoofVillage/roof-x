name: commentable_object_reference

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  commentable_object_type:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: packages
      path: types/commentable_object
  mappable:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: packages
      path: model/mixins/mappable
  key:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: packages
      path: model/key

dev_dependencies:
  flutter_test:
    sdk: flutter
