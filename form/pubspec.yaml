name: form
description: Form tools.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  stream:
    path: ../stream
  network:
    path: ../network
  exceptions:
    path: ../exceptions

dev_dependencies:
  flutter_test:
    sdk: flutter
