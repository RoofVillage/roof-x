name: frequency

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  period_type:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: packages
      path: types/period
  labeled_value:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: packages
      path: labeled_value

dev_dependencies:
  flutter_test:
    sdk: flutter
