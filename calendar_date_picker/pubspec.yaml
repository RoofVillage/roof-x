name: calendar_date_picker
description: Calendar interface for picking a date.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_calendar_carousel: ^1.3.15
  theme:
    path: ../theme
  haptics:
    path: ../haptics
  typography:
    path: ../typography
