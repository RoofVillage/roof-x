enum NotificationUnsubscribeType { landlordTodo }
enum EmailUnsubscribeType { landlordTodo }
enum ContextType { roof, contract, organization }
enum PrivacyType { world }
enum BillingStatusType { verified }
enum BillingType { personal }
enum PaymentDefaultType { notsure }
enum DomainType { roof, org }
enum BankOwnerType { user, org }
enum FrequencyType { daily, weekly, monthly }
enum DayType { sunday, monday, tuesday, wednesday, thursday, friday, saturday }
enum FeePayerType { payer, reciever }
enum InvoiceType { contractual }
