name: types
description: Roof's enum types.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  date:
    path: ../date
  string_from_enum_string:
    path: ../string_from_enum_string

dev_dependencies:
  flutter_test:
    sdk: flutter
