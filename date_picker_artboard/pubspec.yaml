name: date_picker_artboard
description: Artboard that displays a date picker and returns selected dates.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_calendar_carousel: ^1.3.15
  floating_artboard_templates:
    path: "../floating_artboard_templates"
